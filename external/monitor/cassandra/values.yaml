cassandra:
  global:
    storageClass: "nfs-client"
  persistance:
    enabled: true
    storageClass: "nfs-client"
    size: "500Gi"
  dbUser:
    existingSecret:
      name: cassandra-secret
      forcePassword: true
      keyMapping:
        cassandra-password: CASSANDRA_PASSWORD