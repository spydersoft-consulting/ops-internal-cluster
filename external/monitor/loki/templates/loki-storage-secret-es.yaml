apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: loki-storage-secret-es

spec:
  refreshInterval: 1h0m0s
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: loki-storage-secret
  data:
  - secretKey: STORAGE_USERNAME
    remoteRef:
      key: secrets-k8/monitoring/garage/loki
      property: access_key
      conversionStrategy: Default
      decodingStrategy: None
      metadataPolicy: None
  - secretKey: STORAGE_PASSWORD
    remoteRef:
      key: secrets-k8/monitoring/garage/loki
      property: secret_key
      conversionStrategy: Default
      decodingStrategy: None
      metadataPolicy: None